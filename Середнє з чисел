#include <iostream>
#include <tgmath.h>

using namespace std;
int main() {
    int a,b,c;
    cin>>a>>b>>c;
    if(a>=b && a<=c) cout<<a;
    else if(a<=b && a>=c) cout<<a;
    else if(b>=c && b<=a) cout<<b;
    else if(b<=c && b>=a) cout<<b;
    else cout<<c;
    return 0;
}
