#include <iostream>
#include <iomanip>

using namespace std;
int main() {
    string s;
    cin>>s;
    long long sum=0,mul=1;
    for (int i = 0; i < s.size(); ++i) {
        sum+=(s[i]-'0');
        mul*=(s[i]-'0');
    }
    cout<<sum<<" "<<mul;
    return 0;
}
