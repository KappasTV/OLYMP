#include <iostream>
#include <tgmath.h>

using namespace std;
int main() {
    string s;
    cin>>s;
    int sum=0;
    for (int i = 0; i < s.size(); ++i) {
        sum+=pow((s[i]-'0'),2);
    }
    cout<<sum;
    return 0;
}
